# 开发日志 2026年6月 - 8月

> 本文档整理自开发过程中的工作记录，涵盖 2026 年 6 月至 8 月的主要开发活动、问题修复和系统改进。
> 最后更新：2026-08-12

---

## 一、前端界面重构：pyside_v4

### 1.1 设计稿完成（2026-07-17）

创建了完整的前端设计稿系统，位于 `ui_design/` 目录：

- **设计令牌架构**：`tokens.css`（全局基础变量）、`theme-dark.css`（深色主题：琥珀金 + 玻璃拟态）、`theme-light.css`（浅色主题：青蓝 + 云母质感）、`common.css`（公共组件样式）
- **设计稿页面**（共 7 个）：设计稿索引页、深色主界面、浅色主界面、封面墙视图、演员详情窗口、标签管理窗口、扫描进度窗口
- **设计特点**：双主题可切换、虚拟滚动 + 骨架屏懒加载、2:3 竖版封面比例、18 列配置展示

用户选择 **Option A 深色影院风**（琥珀金 + 玻璃拟态）作为主界面方案。

### 1.2 pyside_v4 实现（2026-07-17）

基于设计稿创建了完整的 PySide6 前端界面，目录结构：

- `core/`：数据库连接 + 数据访问层（database.py + repository.py）
- `theme/`：主题系统（colors.py + qss.py）
- `widgets/`：UI 组件（sidebar、video_table、detail_panel、star_rating、filter_bar）
- `windows/`：主窗口（集成数据库、搜索、筛选、排序）
- `dialogs/`：对话框（演员库、标签管理、扫描进度、设置）
- `workers/`：后台任务（数据加载、封面图片加载）

核心功能：数据库连接（49,057 条记录）、异步数据加载（QThread）、搜索（300ms 防抖）+ 筛选 + 排序、封面图片异步加载、写操作（星级评分、标签编辑、演员收藏）。

性能优化：查询从 14 秒降至 9 毫秒（使用索引字段排序），数据库缓存 64MB cache_size。

### 1.3 迁移计划与分阶段实施（2026-07-17）

制定了详细迁移计划（`pyside_v4/MIGRATION_PLAN.md`），从 `media_library_pyside.py` 迁移共 76 项功能。分 6 个阶段完成：

**阶段 1 - 核心后端**：数据库连接、异步加载、搜索、筛选、排序、分页、封面加载

**阶段 2 - 菜单栏**：文件/工具/界面/帮助四个菜单，快捷键（Ctrl+R 刷新、Ctrl+F 搜索、Space 播放、Ctrl+0-5 设置星级）

**阶段 3 - 右键菜单与批量操作**：TaskProgressDialog、单文件/多文件右键菜单、自动标签、生成封面、删除视频

**阶段 4 - 对话框**：Pagination 分页组件、FolderDialog、SmartUpdateDialog、DedupDialog

**阶段 5 - 菜单功能全部实现**：NFO/JAVDB 导入、打分同步、批量 MD5、封面生成、自动标签、文件名清理、文件移动管理、JAVDB 集成、智能去重、设置保存

**阶段 6 - 缺失功能补充**：批量导入 NFO/JAVDB 信息、清理演员信息、重新导入元数据、完全重置数据库、批量标注无标签文件、修正 JAVDB 错误信息、快速智能媒体库更新、JAV 信息面板、快捷键帮助、详情面板增强（13 项）

最终完成：57 个功能模块，完成率 100%。

### 1.4 界面问题修复（2026-07-17）

- **双表头问题**：VideoTable 组件同时使用自定义表头和 QTableWidget 内置表头，修复为只使用内置表头
- **模块导入错误**：`actions/__init__.py` 导入了不存在的模块，修复为只导入实际存在的 VideoActions

---

## 二、MD5 缺失分析与修复（2026-07-16）

### 2.1 问题

数据库 48,888 条记录中 14,775 条缺失 MD5（30.2%），主要集中在 `/Volumes/国产_DX4600`（13,373 条，2026-06-08 导入时未计算 MD5）。

### 2.2 Bug 修复

`fast_smart_media_updater.py`（主文件 + docker 版）：二级回退文件名匹配被 `enable_md5` 错误 gate（第 356 行 `if match_row is None and enable_md5:`），导致不带 `--enable-md5` 时完全不进行移动检测。修复为去掉 `enable_md5` 条件，并增加文件大小验证防止同名不同文件误匹配。

### 2.3 NAS 计算脚本改进

- `md5_calc_nas.sh`：去掉 `set -e`（之前第 81 条大文件 md5sum 失败导致脚本崩溃）、修复路径映射（国产_DX4600 从错误的 `/volume1/` 改为正确的 `/volume4/国产_DX4600/`）、增加单文件超时保护（2 小时）
- `md5_calc_control.py`：改用 `file_path` 前缀匹配、增加路径验证和超时控制

### 2.4 NAS 路径映射（已验证）

| macOS SMB 挂载 | NAS 本地路径 |
|---|---|
| `/Volumes/国产_DX4600/` | `/volume4/国产_DX4600/` |
| `/Volumes/app/usr/` | `/volume1/app/usr/` |
| `/Volumes/HC530_1/` | `/volume2/HC530_1/` |
| `/Volumes/Jav_HDD4/` | `/volume4/Jav_HDD4/` |

### 2.5 任务完成（2026-07-20）

14,770 条任务已提交 DX4600 NAS 后台计算，耗时 7.5 小时，成功计算 13,940 条。数据库 MD5 覆盖率从 69.8% 提升至 97.9%。剩余 830 条缺失 MD5 为 NAS 上已删除的过期记录。

---

## 三、视频批量打标签（2026-06-25 ~ 06-26）

### 3.1 任务概述

对"所有在线且没有标签的视频"批量打标签（tagging，非打分 rating）。模型：`Qwen/Qwen3-VL-30B-A3B-Instruct`，标签合并为 union 模式（只增不减），标签词典 `video_analyzer/vocabulary_tags.txt`（122 标签）。

### 3.2 执行结果

本机 627 个视频：总处理 384，标签更新 354，无标签 30，失败 240（API 限流），耗时 53.4 分钟。失败视频已安排重试。

### 3.3 待办

NAS 挂载后跑剩余 25,000+ 视频。

---

## 四、国产mac 去重清理（2026-06-26）

按 file_size 完全一致找到 132 组重复（264 条记录），每组保留文件名最短的。已执行清理：删除文件 127 个 + 删除数据库记录 132 条 + 5 条仅删记录（文件已不存在）。

---

## 五、NAS 存储迁移（2026-07-18 ~ 07-19）

### 5.1 本机 AV 库迁移到 DXP4800 NAS（07-18）

迁移了 64 个视频到 NAS（收藏演员 → usr 目录，普通演员 → JAV 目录）。演员名到文件夹名以 `merge.conf` 为权威映射。修复迁移脚本改用系统 mv 命令、先移动文件确认成功后再更新 DB。删除 118 条根目录重复 DB 记录，修正 89 条 source_folder 指向。

### 5.2 Essay2T JAV 迁移到 DXP4800（07-19）

从外置 NTFS 盘迁移 59 个番号文件夹 + 1 个替换（去重）。DB 记录更新 44/45 条，新文件入库 199 个。遗留：1 个大文件待迁移、部分收藏演员分类待手动调整、199 个新文件待补充演员关联和标签。

---

## 六、AV 目录清理与视频合并（2026-07-22 ~ 08-09）

### 6.1 AV 目录清理（07-22）

清理路径 `/Users/firewell/影视/AV/`：删除 63 个爬取残留文件、约 82 个广告视频、12 个广告图片/GIF、10 个空文件夹。

### 6.2 视频合并

多次使用 `ffmpeg concat demuxer` 无损合并分片视频：

- 07-22：4 个合集合并为 1 个文件（648MB，时长 01:32:58）
- 07-29：2 个合集合并为 1 个文件（46.8MB，时长 13:40）
- 08-09：2 个分片合并为 1 个文件（12G，时长 70 分 01 秒）

合并后均更新数据库记录并删除源文件。

---

## 七、数据库查询：JAV 多文件/分集分析（2026-08-06）

### 7.1 本地库分析

去重后总文件 35,091；可提取标准番号 5,058 个文件、4,675 个番号。单番号多文件 287 个番号、670 文件，其中真分碟（-CD1/-CD2）111 个番号，其余约 174 个是多格式/多版本并存。

### 7.2 NAS 补充扫描

DXP4800 SMB 已挂载卷扫描 17,266 视频文件，可提取番号 2,382。数据库 29,297 条记录指向 NAS 几乎全覆盖。仅 2 个番号未收录：KPB-023（完全无记录）、IPX-620（缺主文件）。

---

## 八、批量打标签 Bug 修复与路径修正（2026-08-12）

### 8.1 "找不到无标签视频" Bug

根因三层：NAS 卷全部未挂载（25,337 条不可达）、本地目录改名（数据库路径与实际目录不一致）、代码无诊断信息。

修复：查询条件与界面"无标签"分类对齐（新增 `tags = '<无标签>'`）、统计可达/不可达数量、全部不可达时提示具体原因。

### 8.2 路径方向修正

数据库 4,739 条路径已修正，本地 679 条无标签视频全部可达。

### 8.3 API Token 失效修复

两处 token 不一致导致批量分析失败（401 认证错误）。修复为统一环境变量中的有效值。

### 8.4 隐私安全审计

- git remote URL 硬编码凭证已改为不含凭证的 HTTPS URL
- macOS keychain 已清除旧凭证
- git 历史中的失效 token 已用 filter-repo 重写清除
- .gitignore 新增敏感凭证忽略规则

教训：token 只放 shell 环境变量或外部密码管理器，绝不进项目文件夹/remote URL/git 历史。

---

## 九、待办事项汇总

| 待办 | 优先级 | 状态 |
|---|---|---|
| NAS 挂载后跑剩余 25,000+ 视频批量打标签 | 高 | 待 NAS 挂载 |
| Essay2T 遗留 1 个大文件迁移（NTRD-022） | 低 | 待插盘 |
| 199 个新入库文件补充演员关联和标签 | 中 | 待执行 |
| 部分收藏演员分类手动调整 | 低 | 待执行 |
| video_analyzer 引入 python-dotenv 统一配置读取 | 低 | 可选 |

---

*文档整理时间：2026-08-12*
