# 使用工作流
 - 版本: 开发版
 - 最后更新: 2025-11-29
 - 适用系统: Windows/macOS/Linux

## 单条获取 JAV 信息
1. 在列表选中视频 → 详情页点击“获取JAV信息”。
2. 系统自动提取番号、搜索并保存到数据库。
3. 列表刷新显示 `javdb_info` 字段。

## 批量导入（无标题）
1. 菜单“文件”→“批量导入JAV信息”。
2. 系统遍历无标题视频，逐条提取番号、搜索并保存；状态栏显示进度。

## 手动输入
1. 菜单“工具”→“JAV信息面板”。
2. 输入番号，查看结果；若当前选中视频存在，可直接保存。
