# 开发笔记和问题追踪

## 📝 开发历程

### 2025年12月 - PySide6 版本功能补全与重构 (v2.0)

#### 背景
`media_library_pyside.py` 是基于 PySide6 的现代化重写版本，但长期以来缺失了 Tkinter 版本中的许多高级功能（批量操作、维护工具等），且核心逻辑与 GUI 耦合严重，导致代码膨胀和维护困难。

#### 目标
1.  **功能对齐**: 补全 PySide6 版本缺失的所有核心功能。
2.  **架构解耦**: 将业务逻辑抽离到独立的 `utils` 模块。
3.  **体验优化**: 使用后台线程处理耗时任务，避免 UI 卡顿。

#### 实施步骤

1.  **Utils 模块设计**:
    *   `batch_ops.py`: 封装批量 MD5、NFO/JavDB 导入、文件移动等逻辑。
    *   `maintenance.py`: 封装去重、星级同步、数据清理等维护逻辑。
    *   `thumbnails.py`: 独立的缩略图生成器。
    *   **Wrapper 模式**: 为解决 `media_library.py` 与新模块间的循环引用，创建了 `code_extractor.py` 和 `javsp_integration.py` 的 wrapper。

2.  **GUI 集成**:
    *   重写 `show_context_menu`，支持多选和动态菜单。
    *   使用 `GenericWorker` 模式统一处理异步任务。
    *   实现 `FileMoveDialog`，移植了旧版的文件移动管理器 UI。

3.  **遇到的挑战**:
    *   **循环导入**: `media_library.py` 依赖根目录的许多脚本，而这些脚本又可能依赖 `media_library.py` 的某些常量。解决方案是尽量减少在 `utils` 中直接引用主程序，而是传入 `DatabaseManager` 或 `cursor`。
    *   **线程安全**: SQLite 在多线程环境下需要小心处理连接。我们在 `BatchOperationManager` 中使用了传入的 `db_manager`，确保在 worker 线程中正确使用数据库连接（或者在主线程提交，视具体实现而定，v2.0 中采用了 worker 内部处理逻辑）。
    *   **代码复用**: 旧版 Tkinter 代码大量使用了 `self` 引用，移植时需要仔细剥离 UI 依赖。

#### 成果
*   PySide6 版本现在是一个功能完备的替代品。
*   代码结构更加清晰，`utils` 模块可被其他脚本复用。
*   新增了详细的单元测试 `tests/test_pyside_utils.py`。

### 2024年 - JAVDB标题映射问题

#### 问题发现
- **触发**：用户反馈NFO文件导入后JAVDB标题显示异常
- **症状**：javdb_title字段与title字段不一致
- **影响**：所有通过NFO文件导入的媒体条目

#### 调查过程

1. **代码分析阶段**
   - 定位到`media_library.py`中的`parse_nfo_file()`函数
   - 发现自动分割标题的逻辑（约9847行）
   - 确认影响范围：所有NFO导入功能

2. **逻辑分析**
   ```python
   # 原有问题逻辑
   if ' ' in full_title:
       parts = full_title.split(' ', 1)
       nfo_data['code'] = parts[0]  # 番号
       nfo_data['javdb_title'] = parts[1]  # 移除了番号部分
   ```
   
   **问题**：javdb_title被强制分割，移除番号信息

3. **用户期望调研**
   - 用户希望javdb_title直接映射title内容
   - 不希望自动分割标题
   - 保持标题完整性

#### 解决方案探索

1. **直接映射方案**
   ```python
   # 推荐方案
   nfo_data['javdb_title'] = full_title
   ```

2. **配置化方案**（备选）
   ```python
   if config.get('auto_split_javdb_title', False):
       # 分割逻辑
   else:
       # 直接映射
   ```

3. **智能检测方案**（备选）
   ```python
   if should_split_intelligently(full_title):
       # 智能分割
   else:
       # 保持完整
   ```

#### 实施过程

1. **测试验证**
   - 创建`test_nfo_mapping.py`测试脚本
   - 验证5种不同标题格式
   - 确认修改后的逻辑符合预期

2. **代码修改尝试**
   - 修改`parse_nfo_file()`函数逻辑
   - 移除自动分割代码
   - 实现直接映射

3. **遇到的问题**
   - 缩进错误导致测试失败
   - 多次修改和调试
   - 最终决定暂停修改，先完善文档

#### 当前状态

- ✅ 问题根因已定位
- ✅ 解决方案已确定（直接映射）
- ✅ 测试脚本已完成
- ✅ 文档已完善
- ⏳ 代码修改待用户确认后实施

## 🔍 技术细节记录

### 相关文件位置
```
/Users/firewell/Library/CloudStorage/OneDrive-个人/bioinfo/media/
├── media_library.py          # 核心逻辑文件
├── test_nfo_mapping.py      # 测试脚本（新建）
└── doc/                     # 文档目录（新建）
    ├── javdb_title_mapping_analysis.md
    ├── javdb_title_mapping_solutions.md
    ├── project_documentation_index.md
    └── development_notes.md
```

### 关键函数位置
- `parse_nfo_file()` - 约9847行
- `auto_import_nfo_for_video()` - 约3550行  
- `import_nfo_from_context()` - 约9710行

### 测试用例记录

#### 测试标题格式
1. `'IPX-123 美咲かんな 新人デビュー'` - 标准格式
2. `'IPX123 美咲かんな 新人デビュー'` - 无分隔符
3. `'美咲かんな 新人デビュー'` - 无番号
4. `'IPX-123'` - 纯番号
5. `'TEST-456 这是测试标题'` - 测试格式

#### 测试结果对比
| 标题格式 | 原逻辑javdb_title | 新逻辑javdb_title | 状态 |
|---------|------------------|------------------|------|
| 标准格式 | 美咲かんな 新人デビュー | IPX-123 美咲かんな 新人デビュー | ✅ |
| 无分隔符 | 美咲かんな 新人デビュー | IPX123 美咲かんな 新人デビュー | ✅ |
| 无番号 | 美咲かんな 新人デビュー | 美咲かんな 新人デビュー | ✅ |
| 纯番号 | IPX-123 | IPX-123 | ✅ |
| 测试格式 | 这是测试标题 | TEST-456 这是测试标题 | ✅ |

## 🎯 经验教训

### 开发经验
1. **问题定位要准确**
   - 通过用户反馈快速定位核心问题
   - 深入分析代码逻辑，找到根本原因
   - 考虑问题的全面影响范围

2. **解决方案要对比**
   - 不要急于实施第一个想到的方案
   - 对比多种解决方案的优缺点
   - 考虑短期和长期的可行性

3. **测试验证要充分**
   - 创建专门的测试脚本
   - 覆盖各种边界情况
   - 确保修改不会引入新问题

4. **文档记录要完整**
   - 详细记录问题分析过程
   - 保存解决方案对比
   - 为后续维护提供参考

### 技术要点
1. **NFO文件格式**
   - 理解XML结构和标签含义
   - 明确title标签的内容格式
   - 考虑番号提取的准确性

2. **数据库更新逻辑**
   - 理解javdb_title的更新机制
   - 考虑与现有数据的兼容性
   - 评估回滚方案的可行性

3. **用户期望管理**
   - 明确用户的核心需求
   - 平衡技术实现和用户期望
   - 提供清晰的变更说明

## 🔮 后续计划

### 短期计划
1. **等待用户决策**
   - 用户考虑后决定是否实施修改
   - 根据用户反馈调整方案
   - 确认实施的具体时间点

2. **完善文档**
   - 补充技术细节文档
   - 创建用户使用指南
   - 准备发布说明

### 中期计划
1. **实施代码修改**
   - 根据确定的方案修改代码
   - 全面测试验证
   - 更新相关文档

2. **用户支持**
   - 监控用户反馈
   - 解答使用问题
   - 收集改进建议

### 长期计划
1. **功能优化**
   - 根据用户反馈优化逻辑
   - 考虑实现配置化选项
   - 完善番号提取算法

2. **架构改进**
   - 考虑模块化设计
   - 提高代码可维护性
   - 优化性能表现

## 📋 待办事项

### 高优先级
- [ ] 等待用户确认修改方案
- [ ] 准备代码修改实施
- [ ] 完善测试用例覆盖

### 中优先级
- [ ] 创建用户使用文档
- [ ] 准备发布说明
- [ ] 设计回滚方案

### 低优先级
- [ ] 优化番号提取逻辑
- [ ] 考虑配置化实现
- [ ] 改进代码架构

---

*记录时间：2024年*
*最后更新：文档创建时*