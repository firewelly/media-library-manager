# 从 Tk 版到 PySide6 版迁移说明
 - 版本: 开发版
 - 最后更新: 2025-11-29
 - 适用系统: Windows/macOS/Linux

## 原则
- 不修改 `media_library.py`；PySide6 通过 `utils/` 复用逻辑。
- 数据库保持不变，仅连接现有库。

## 差异点
- 日志输出由 Qt 信号适配；详情页与菜单结构不同，但功能一致。
- 部分功能暂以占位实现（媒体信息、缩略图），后续逐步接入原有实现。
