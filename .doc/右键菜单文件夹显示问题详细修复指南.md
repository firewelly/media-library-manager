# 右键菜单文件夹显示问题详细修复指南

## 问题确认
通过查看 `media_library.py` 第6000-6100行的代码，确认了以下问题：

1. **只显示基本名称**：代码中使用了 `os.path.basename(folder_path)`，只显示文件夹的基本名称
2. **没有在线状态检查**：只查询了 `is_active = 1` 的文件夹，但没有检查文件夹是否实际可访问
3. **重复代码**：相同的逻辑在多个地方重复出现

## 问题代码位置

### 1. 单文件右键菜单（约6020-6035行）
```python
# 迁移JavSP到
migrate_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label="迁移JavSP到", menu=migrate_menu)
self.cursor.execute("SELECT DISTINCT folder_path FROM folders WHERE is_active = 1 ORDER BY folder_path")
for row in self.cursor.fetchall():
    _folder_path = row[0]
    _folder_name = os.path.basename(_folder_path)  # 只显示基本名称
    migrate_menu.add_command(label=_folder_name,
                           command=lambda fp=_folder_path: self.migrate_javsp_file_to_library(video_info['id'], video_info['path'], fp))

# 复制JavSP到
copy_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label="复制JavSP到", menu=copy_menu)
self.cursor.execute("SELECT DISTINCT folder_path FROM folders WHERE is_active = 1 ORDER BY folder_path")
for row in self.cursor.fetchall():
    _folder_path = row[0]
    _folder_name = os.path.basename(_folder_path)  # 只显示基本名称
    copy_menu.add_command(label=_folder_name,
                         command=lambda fp=_folder_path: self.copy_javsp_file_to_library(video_info['id'], video_info['path'], fp))
```

### 2. 批量操作菜单（约6070-6090行）
```python
# 批量迁移JavSP到
migrate_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label=f"批量迁移JavSP到 ({len(selected_videos)}个文件)", menu=migrate_menu)
self.cursor.execute("SELECT DISTINCT folder_path FROM folders WHERE is_active = 1 ORDER BY folder_path")
for row in self.cursor.fetchall():
    _folder_path = row[0]
    _folder_name = os.path.basename(_folder_path)  # 只显示基本名称
    migrate_menu.add_command(label=_folder_name,
                           command=lambda fp=_folder_path: self.batch_migrate_javsp_files_to_library(fp))

# 批量复制JavSP到
copy_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label=f"批量复制JAVSP到 ({len(selected_videos)}个文件)", menu=copy_menu)
self.cursor.execute("SELECT DISTINCT folder_path FROM folders WHERE is_active = 1 ORDER BY folder_path")
for row in self.cursor.fetchall():
    _folder_path = row[0]
    _folder_name = os.path.basename(_folder_path)  # 只显示基本名称
    copy_menu.add_command(label=_folder_name,
                         command=lambda fp=_folder_path: self.batch_copy_javsp_files_to_library(fp))
```

## 完整修复方案

### 第一步：添加辅助方法
在 `media_library.py` 的适当位置（建议在其他辅助方法附近）添加以下方法：

```python
def get_online_folders(self):
    """获取所有在线的活跃文件夹"""
    try:
        self.cursor.execute("SELECT DISTINCT folder_path FROM folders WHERE is_active = 1 ORDER BY folder_path")
        all_folders = [row[0] for row in self.cursor.fetchall()]
        
        # 只返回在线（可访问）的文件夹
        online_folders = []
        for folder_path in all_folders:
            if os.path.exists(folder_path) and os.path.isdir(folder_path):
                online_folders.append(folder_path)
        
        return online_folders
    except Exception as e:
        print(f"获取在线文件夹失败: {e}")
        return []

def format_folder_display_name(self, folder_path):
    """格式化文件夹显示名称"""
    # 获取文件夹的基本名称
    base_name = os.path.basename(folder_path)
    
    # 如果基本名称太短或太通用，显示更多路径信息
    if len(base_name) <= 3 or base_name.lower() in ['movies', 'videos', 'films', 'video', 'movie', 'film']:
        # 显示父目录和当前目录
        parent_dir = os.path.basename(os.path.dirname(folder_path))
        if parent_dir:
            return f"{parent_dir}/{base_name}"
        else:
            return base_name
    
    return base_name
```

### 第二步：修改单文件右键菜单代码
替换原有的单文件右键菜单创建逻辑（约6020-6035行）：

```python
# 迁移JavSP到
migrate_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label="迁移JavSP到", menu=migrate_menu)

# 获取在线文件夹
online_folders = self.get_online_folders()
for folder_path in online_folders:
    display_name = self.format_folder_display_name(folder_path)
    migrate_menu.add_command(label=display_name,
                           command=lambda fp=folder_path: self.migrate_javsp_file_to_library(video_info['id'], video_info['path'], fp))

# 复制JavSP到
copy_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label="复制JavSP到", menu=copy_menu)

for folder_path in online_folders:
    display_name = self.format_folder_display_name(folder_path)
    copy_menu.add_command(label=display_name,
                         command=lambda fp=folder_path: self.copy_javsp_file_to_library(video_info['id'], video_info['path'], fp))
```

### 第三步：修改批量操作菜单代码
替换原有的批量操作菜单创建逻辑（约6070-6090行）：

```python
# 批量迁移JavSP到
migrate_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label=f"批量迁移JavSP到 ({len(selected_videos)}个文件)", menu=migrate_menu)

# 获取在线文件夹
online_folders = self.get_online_folders()
for folder_path in online_folders:
    display_name = self.format_folder_display_name(folder_path)
    migrate_menu.add_command(label=display_name,
                           command=lambda fp=folder_path: self.batch_migrate_javsp_files_to_library(fp))

# 批量复制JavSP到
copy_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label=f"批量复制JAVSP到 ({len(selected_videos)}个文件)", menu=copy_menu)

for folder_path in online_folders:
    display_name = self.format_folder_display_name(folder_path)
    copy_menu.add_command(label=display_name,
                         command=lambda fp=folder_path: self.batch_copy_javsp_files_to_library(fp))
```

### 第四步：修改"移动到"菜单（可选）
同样的修复也可以应用到"移动到"菜单（约6040-6050行）：

```python
# 添加移动到子菜单
move_menu = tk.Menu(context_menu, tearoff=0)
context_menu.add_cascade(label=f"批量移动到 ({len(selected_videos)}个文件)", menu=move_menu)

# 获取在线文件夹
online_folders = self.get_online_folders()
for folder_path in online_folders:
    display_name = self.format_folder_display_name(folder_path)
    move_menu.add_command(label=display_name, 
                        command=lambda fp=folder_path: self.batch_move_files_to_folder(fp))
```

## 修复后的效果

1. **显示更友好的文件夹名称**：
   - 通用名称如 "movies" → "parent/movies"
   - 特定名称如 "JAV-2024" → "JAV-2024"

2. **只显示在线文件夹**：
   - 自动过滤掉不可访问的文件夹
   - 避免用户选择无效的目标文件夹

3. **代码更加简洁**：
   - 消除了重复的数据库查询代码
   - 统一的文件夹显示格式化逻辑

## 测试验证

1. **创建测试环境**：
   - 创建几个具有通用名称的文件夹（如 "movies", "videos"）
   - 创建一些具有特定名称的文件夹（如 "JAV-2024", "收藏"）

2. **测试离线状态**：
   - 临时重命名或移动一些文件夹
   - 验证这些文件夹不会出现在右键菜单中

3. **验证显示效果**：
   - 检查右键菜单中文件夹名称的显示格式
   - 验证批量操作菜单的显示一致性

## 注意事项

1. **性能考虑**：`get_online_folders` 方法会检查每个文件夹的存在性，对于大量文件夹可能会有轻微的性能影响
2. **错误处理**：添加了异常处理，确保即使数据库查询失败也不会导致程序崩溃
3. **向后兼容**：修复不会影响现有的功能逻辑，只是改进了显示和过滤